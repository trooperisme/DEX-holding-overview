# DEX Holding Overview

Separate DEX holdings project for Zapper-based portfolio aggregation and dashboarding.

## Seed data

- `data/raw/dex-entities-zapper.csv`: input table of `entity_name` + `full_zapper_link`
